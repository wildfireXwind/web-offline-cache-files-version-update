In windows, create a bat file in your program dir and type follow text:

@echo off
@node your app.js path
pause

run this bat, then it would update the URL of all resource file in the program.

if you didn't understood how to wrote the bat file, you could saw the test.bat.